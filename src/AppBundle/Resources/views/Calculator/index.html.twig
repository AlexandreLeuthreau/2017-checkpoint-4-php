{% extends '@Subject/layout.html.twig' %}


{% block title %}AppBundle:Calculator:index{% endblock %}

	{% block body %}
		<div class="container">
			<div class="row">
				<div class="panel panel-default">
					<div class="panel-heading">
						<h3 class="panel-title">My Calculator</h3>
					</div>
					<div class="panel-body">
						{{ form(calculator_form)}}
					</div>
					<div class="panel-footer">
						{% for key,value in order_values %}
							<p>Montant {{ key|upper }} : <strong>{{ value|number_format(2, '.', ',') }}€</strong></p>
						{% else %}
							--
						{% endfor %}
					</div>
				</div>
			</div>
		</div>
	{% endblock %}