subject:
    resource: "@SubjectBundle/Controller/"
    type:     annotation
    prefix:   /

app:
    resource: '@AppBundle/Controller/'
    type: annotation
    prefix: /calc
    defaults: { _controller: AppBundle:Default:calc }
